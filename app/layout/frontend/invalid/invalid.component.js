angular.module('invalid').component('invalid', {
    templateUrl: 'layout/frontend/invalid/invalid.template.html',
    controller: {
        function($http) {
            return true;
        }
    }
});