// Registra il componente 'loginlogon' sul modulo 'loginlogon'
angular.module('loginlogon').component('loginlogon', {
    templateUrl: 'layout/frontend/loginlogon/loginlogon.template.html',
    controller: {
        function($http) {
            return true;
        }
    }
});